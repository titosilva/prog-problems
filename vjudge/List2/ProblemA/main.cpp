#include <bits/stdc++.h>

using namespace std;

int emulate(int n) {
    for (int p = 2; p < n / 2; p++) {
        auto c = n;
        while (c % p != 0) {
            
        }
    }
}

int main() {
    int n = 25;
    n = 3;
    return 0;
}
