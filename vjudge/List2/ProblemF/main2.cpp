#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;

    cin >> N;
    if (N == 1) {
        cout << 0 << endl;
        return 0;
    }

    auto nums = vector<int>(N);
    for (auto &n: nums) {
        cin >> n;
    }

    
   
    return 0;
}
